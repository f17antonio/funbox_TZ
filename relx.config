%% -*- mode: Erlang; -*-
{lib_dirs, ["deps"]}.
{sys_config, "etc/fbtz.config"}.
{vm_args, "etc/vm.args"}.
{include_erts, false}.
{extended_start_script, true}.
{release, {fbtz, semver},
 [
   fbtz,
   eredis,
   kernel,
   stdlib,
   sasl,
   lager
 ]}.
